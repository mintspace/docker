# 7. Get the internal IP address of the overlord container without starting its shell and
# in one command.

docker inspect -f '{{.NetworkSettings.IPAddress}}' overlord

# Get info: docker inspect -s overlord // to get all list of options